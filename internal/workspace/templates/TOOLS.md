# Tool Notes

Use the registered tools instead of inventing capabilities.

## General

- Prefer read_file and list_dir for workspace inspection.
- Use terminal for non-interactive shell commands inside the workspace.
- Use message to proactively send user-facing updates when needed.

## Skills

- get_skill loads a workspace skill from skills/<name>/SKILL.md.